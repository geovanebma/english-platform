from pathlib import Path
import re

BASE = Path(r"C:\Users\Geovane TI\Documents\english-platform")

def read_text(path):
    for enc in ("utf-8", "cp1252", "latin-1"):
        try:
            return Path(path).read_text(encoding=enc)
        except Exception:
            pass
    raise RuntimeError(f"Cannot read {path}")

def write_text(path, text):
    Path(path).write_text(text, encoding="utf-8", newline="\n")

def patch_db():
    path = BASE / "server" / "db.js"
    text = read_text(path)
    text = text.replace(
"""  `CREATE TABLE IF NOT EXISTS english_ui_labels (\n    id TEXT PRIMARY KEY,\n    label_key TEXT NOT NULL,\n    locale TEXT NOT NULL,\n    text_value TEXT NOT NULL,\n    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    UNIQUE(label_key, locale)\n  )`,\n  `CREATE INDEX IF NOT EXISTS idx_english_telemetry_events_user ON english_telemetry_events(user_id)`,\n""",
"""  `CREATE TABLE IF NOT EXISTS english_ui_labels (\n    id TEXT PRIMARY KEY,\n    label_key TEXT NOT NULL,\n    locale TEXT NOT NULL,\n    text_value TEXT NOT NULL,\n    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    UNIQUE(label_key, locale)\n  )`,\n  `CREATE TABLE IF NOT EXISTS english_community_posts (\n    id TEXT PRIMARY KEY,\n    author_user_id TEXT REFERENCES english_users(id) ON DELETE SET NULL,\n    author_key TEXT NOT NULL,\n    author_name TEXT NOT NULL,\n    content TEXT NOT NULL,\n    media_url TEXT NOT NULL DEFAULT '',\n    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()\n  )`,\n  `CREATE TABLE IF NOT EXISTS english_community_comments (\n    id TEXT PRIMARY KEY,\n    post_id TEXT NOT NULL REFERENCES english_community_posts(id) ON DELETE CASCADE,\n    author_user_id TEXT REFERENCES english_users(id) ON DELETE SET NULL,\n    author_key TEXT NOT NULL,\n    author_name TEXT NOT NULL,\n    text TEXT NOT NULL,\n    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()\n  )`,\n  `CREATE TABLE IF NOT EXISTS english_community_post_likes (\n    post_id TEXT NOT NULL REFERENCES english_community_posts(id) ON DELETE CASCADE,\n    user_id TEXT NOT NULL REFERENCES english_users(id) ON DELETE CASCADE,\n    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    PRIMARY KEY (post_id, user_id)\n  )`,\n  `CREATE TABLE IF NOT EXISTS english_community_reports (\n    id TEXT PRIMARY KEY,\n    post_id TEXT NOT NULL REFERENCES english_community_posts(id) ON DELETE CASCADE,\n    reporter_user_id TEXT NOT NULL REFERENCES english_users(id) ON DELETE CASCADE,\n    reason TEXT NOT NULL DEFAULT 'general',\n    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    UNIQUE(post_id, reporter_user_id)\n  )`,\n  `CREATE TABLE IF NOT EXISTS english_community_user_mutes (\n    id TEXT PRIMARY KEY,\n    muter_user_id TEXT NOT NULL REFERENCES english_users(id) ON DELETE CASCADE,\n    muted_author_key TEXT NOT NULL,\n    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n    UNIQUE(muter_user_id, muted_author_key)\n  )`,\n  `CREATE INDEX IF NOT EXISTS idx_english_telemetry_events_user ON english_telemetry_events(user_id)`,\n""")
    text = text.replace(
"""  `CREATE INDEX IF NOT EXISTS idx_english_wiki_words_normalized ON english_wiki_words(normalized_word)`,\n  `CREATE INDEX IF NOT EXISTS idx_english_ui_labels_locale ON english_ui_labels(locale)`,\n];\n""",
"""  `CREATE INDEX IF NOT EXISTS idx_english_wiki_words_normalized ON english_wiki_words(normalized_word)`,\n  `CREATE INDEX IF NOT EXISTS idx_english_ui_labels_locale ON english_ui_labels(locale)`,\n  `CREATE INDEX IF NOT EXISTS idx_english_community_posts_created_at ON english_community_posts(created_at DESC)`,\n  `CREATE INDEX IF NOT EXISTS idx_english_community_comments_post ON english_community_comments(post_id, created_at ASC)`,\n  `CREATE INDEX IF NOT EXISTS idx_english_community_reports_post ON english_community_reports(post_id)`,\n];\n""")
    text = text.replace(
        '["profile.course_fallback", "en-US", "Course"],',
        '''["profile.course_fallback", "en-US", "Course"],\n    ["menu.content_admin", "pt-BR", "Admin de conteudo"],\n    ["menu.content_admin", "en-US", "Content admin"],\n    ["dashboard.admin_copy", "pt-BR", "Edite labels do app e revise entradas do dicionario em um painel interno."],\n    ["dashboard.admin_copy", "en-US", "Edit app labels and review dictionary entries from one internal panel."],\n    ["dashboard.open_admin", "pt-BR", "Abrir admin"],\n    ["dashboard.open_admin", "en-US", "Open admin"],\n    ["community.real_backend", "pt-BR", "Feed em nuvem multiusuario"],\n    ["community.real_backend", "en-US", "Multi-user cloud feed"],\n    ["community.empty_real", "pt-BR", "Ainda nao ha posts neste filtro."],\n    ["community.empty_real", "en-US", "There are no posts in this filter yet."],\n    ["community.refresh", "pt-BR", "Atualizar"],\n    ["community.refresh", "en-US", "Refresh"],\n    ["community.comment_publish", "pt-BR", "Comentar"],\n    ["community.comment_publish", "en-US", "Comment"],\n    ["community.like_toggle", "pt-BR", "Curtir"],\n    ["community.like_toggle", "en-US", "Like"],\n    ["community.feed_error", "pt-BR", "Nao foi possivel carregar o feed real agora."],\n    ["community.feed_error", "en-US", "Could not load the live feed right now."],\n    ["community.author_you", "pt-BR", "Voce"],\n    ["community.author_you", "en-US", "You"],\n    ["admin.kicker", "pt-BR", "CONTENT OPS"],\n    ["admin.kicker", "en-US", "CONTENT OPS"],\n    ["admin.title", "pt-BR", "Painel de conteudo"],\n    ["admin.title", "en-US", "Content admin panel"],\n    ["admin.subtitle", "pt-BR", "Revise dicionario, refaca meanings e ajuste labels do app sem sair da plataforma."],\n    ["admin.subtitle", "en-US", "Review dictionary data, rerun meanings, and adjust app labels without leaving the platform."],\n    ["admin.tab.dictionary", "pt-BR", "Dicionario"],\n    ["admin.tab.dictionary", "en-US", "Dictionary"],\n    ["admin.tab.labels", "pt-BR", "Labels"],\n    ["admin.tab.labels", "en-US", "Labels"],\n    ["admin.search", "pt-BR", "Buscar"],\n    ["admin.search", "en-US", "Search"],\n    ["admin.loading", "pt-BR", "Carregando..."],\n    ["admin.loading", "en-US", "Loading..."],\n    ["admin.empty", "pt-BR", "Nenhum resultado encontrado."],\n    ["admin.empty", "en-US", "No results found."],\n    ["admin.select_entry", "pt-BR", "Selecione uma entrada para editar."],\n    ["admin.select_entry", "en-US", "Select an entry to edit."],\n    ["admin.select_label", "pt-BR", "Selecione uma label para editar."],\n    ["admin.select_label", "en-US", "Select a label to edit."],\n    ["admin.save", "pt-BR", "Salvar"],\n    ["admin.save", "en-US", "Save"],\n    ["admin.reverify", "pt-BR", "Refazer meanings"],\n    ["admin.reverify", "en-US", "Reverify meanings"],\n    ["admin.locale", "pt-BR", "Idioma"],\n    ["admin.locale", "en-US", "Locale"],\n    ["admin.results", "pt-BR", "Resultados"],\n    ["admin.results", "en-US", "Results"],\n    ["admin.text_value", "pt-BR", "Texto"],\n    ["admin.text_value", "en-US", "Text value"],\n    ["admin.saved", "pt-BR", "Salvo com sucesso."],\n    ["admin.saved", "en-US", "Saved successfully."],\n    ["admin.invalid_json", "pt-BR", "JSON invalido em um dos campos."],\n    ["admin.invalid_json", "en-US", "Invalid JSON in one of the fields."],\n    ["admin.dict.meanings", "pt-BR", "Meanings (JSON)"],\n    ["admin.dict.meanings", "en-US", "Meanings (JSON)"],\n    ["admin.dict.examples", "pt-BR", "Exemplos (JSON)"],\n    ["admin.dict.examples", "en-US", "Examples (JSON)"],\n    ["admin.dict.synonyms", "pt-BR", "Sinonimos (JSON)"],\n    ["admin.dict.synonyms", "en-US", "Synonyms (JSON)"],\n    ["admin.dict.part_of_speech", "pt-BR", "Classe gramatical"],\n    ["admin.dict.part_of_speech", "en-US", "Part of speech"],\n    ["admin.dict.ipa", "pt-BR", "IPA"],\n    ["admin.dict.ipa", "en-US", "IPA"],\n    ["admin.dict.audio_url", "pt-BR", "Audio URL"],\n    ["admin.dict.audio_url", "en-US", "Audio URL"],\n    ["admin.dict.verified", "pt-BR", "Verificado"],\n    ["admin.dict.verified", "en-US", "Verified"],'''
    )
    seed_fn = '''\nasync function seedCommunityPosts() {\n  const existing = await pool.query(`SELECT COUNT(*)::int AS total FROM english_community_posts`);\n  if (Number(existing.rows[0]?.total || 0) > 0) {\n    return;\n  }\n\n  const posts = [\n    { id: "seed_post_ana", author_key: "user_ana", author_name: "Ana", content: "Today I learned 12 new words with context sentences. Huge difference.", media_url: "", created_at: "2026-03-05T12:15:00.000Z" },\n    { id: "seed_post_lu", author_key: "user_lu", author_name: "Lu", content: "Anyone wants to practice short speaking drills tonight?", media_url: "", created_at: "2026-03-05T17:05:00.000Z" },\n    { id: "seed_post_noah", author_key: "user_noah", author_name: "Noah", content: "Tip: shadowing for 10 minutes a day improved my listening speed.", media_url: "", created_at: "2026-03-06T07:30:00.000Z" },\n  ];\n\n  for (const post of posts) {\n    await pool.query(\n      `INSERT INTO english_community_posts (id, author_key, author_name, content, media_url, created_at)\n       VALUES ($1, $2, $3, $4, $5, $6)\n       ON CONFLICT (id) DO NOTHING`,\n      [post.id, post.author_key, post.author_name, post.content, post.media_url, post.created_at]\n    );\n  }\n\n  await pool.query(\n    `INSERT INTO english_community_comments (id, post_id, author_key, author_name, text, created_at)\n     VALUES ($1, $2, $3, $4, $5, $6)\n     ON CONFLICT (id) DO NOTHING`,\n    ["seed_comment_caio", "seed_post_ana", "user_caio", "Caio", "Nice progress. Context helps a lot.", "2026-03-05T13:00:00.000Z"]\n  );\n}\n\n'''
    text = text.replace('export async function ensureDatabase() {', seed_fn + 'export async function ensureDatabase() {')
    text = text.replace('  await seedWikiWords();\n  await seedUiLabels();', '  await seedCommunityPosts();\n  await seedWikiWords();\n  await seedUiLabels();')
    write_text(path, text)

patch_db()
